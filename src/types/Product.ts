export interface Product {
  id: number,
  title: string,
  description: string,
  price: string,
  discountPercentage: number,
  stock: number,
  brand: string,
  category: string,
  thumbnail: string,
  images: string[],
}
